stages:
- name: uploadArchives
  steps:
  - runScriptConfig:
      image: gradle:jdk8
      shellScript: |-
        java -version
        #gradle clean uploadArchives closeAndReleaseRepository
timeout: 60
