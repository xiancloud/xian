stages:
- name: uploadArchives
  steps:
  - runScriptConfig:
      image: java:8
      shellScript: |-
        java -version
        #gradle clean uploadArchives closeAndReleaseRepository
timeout: 60
